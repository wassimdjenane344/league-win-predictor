global:
  scrape_interval: 15s

scrape_configs:
  # Scrapes the live production backend on Render (not a local container).
  # Launch with docker-compose.monitoring.yml.
  - job_name: "flask_backend_prod"
    scheme: https
    metrics_path: /metrics
    static_configs:
      - targets: ["lol-backend-awex.onrender.com"]
